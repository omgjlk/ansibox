---
homebrew:
  taps:
    - caskroom/cask
  packages:
    - brew-cask
    - ansible
    #- vim # vim is broken, tabs won't work?
    - zsh
    - git
    - bash-completion
    - ssh-copy-id
  casks:
    - iterm2
    - spotify
